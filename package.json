{
  "name": "php-docker-cc",
  "version": "1.0.0",
  "main": "index.php",
  "type": "module",
  "scripts": {
    "start": "php -S localhost:8000 -t public",
    "start:docker": "docker-compose up -d",
    "test": "echo \"Error: no test specified\" && exit 1",
    "node-test": "node --test node_test/*.js",
    "jest-test": "jest"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@babel/preset-env": "^7.27.2",
    "jest": "^29.7.0"
  }
}